version: "3"
services:
  controller:
    build: .
    container_name: videowall-controller
    ports:
      - "8088:8088"
    restart: unless-stopped
    volumes:
      - ./data:/app/data
      - ./public:/app/public
